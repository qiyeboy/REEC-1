#ifndef _REEC_H_
#define _REEC_H_

#define low_alpha(x)		('a' <= x && x <= 'z')
#define cap_alpha(x)		('A' <= x && x <= 'Z')
#define digit(x)			('0' <= x && x <= '9')

typedef enum TregExpression{
	
	_match_low_alpha_norm_limit_pattern_,		// <(a-z)^1>
	_match_low_alpha_
	
	
	
	
	
	
	
}_regular_expression_









#endif

 
 